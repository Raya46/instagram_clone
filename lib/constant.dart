import "package:flutter/material.dart";

const bgColor = Color.fromRGBO(0, 0, 0, 1.0);
const but = Color.fromRGBO(48, 48, 48, 0.498);
const blueColor = Color.fromRGBO(0, 149, 246, 1.0);
const primaryColor = Colors.white;
const secondaryColor = Colors.grey;
const darkGreyColor = Color.fromRGBO(97, 97, 97, 1);
const transparant = Color(0x00000000);

Widget SizeVertical(double height){
  return SizedBox(height: height,);
}

Widget SizeHorizontal(double width){
  return SizedBox(width: width,);
}